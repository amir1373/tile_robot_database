version: "3.8"

services:
  db:
    image: postgres:latest
    ports:
      - "8080:5432"
    environment:
      POSTGRES_USER: "emad"
      POSTGRES_PASSWORD: "1234"
      POSTGRES_DB: "test"
    volumes:
      - db_volume:/var/lib/postgresql/data


volumes:
  db_volume:
    name: db_v